#!/bin/sh
# runit stage 2 — runsvdir supervision
# All binaries are in /bin

PATH=/bin

# Brief pause to let devtmpfs settle before starting services
sleep 1
exec runsvdir -P /etc/service 2>/dev/null
